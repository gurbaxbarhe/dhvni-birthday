{
  "name": "dhvni-birthday",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "encrypt": "node scripts/encryptBundle.mjs",
    "lint": "tsc && prettier --check .",
    "lint:fix": "prettier --write ."
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "prettier": "^3.3.0"
  }
}
